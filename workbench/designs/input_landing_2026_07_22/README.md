# Input landing — 2026-07-22

This record describes the provenance-only landing from the read-only dirty
checkout at `ea5539f3372e1bad5ad05eba85f4bd9a53e8c868` onto the clean baseline
`origin/main` at `73b29e51434e4a139efcb09574f8dc1c94bd485f`.

No CAD was generated, repaired, refactored, or visually re-approved. Python
remains authoritative. The copied STEP and JSON files are immutable reference
evidence, not source and not proof that the landed source rebuilds identical
geometry.

## Records

- `brief.md` — verbatim user request.
- `contract.md` — scope, invariants, and measurable acceptance conditions.
- `intake_manifest.csv` — file-by-file source identity, disposition, owner,
  dependency role, classification, and evidence-link mapping.
- `dependency_closure.md` — the selected import and runtime-source closure.
- `exclusions.md` — dirty-checkout changes deliberately left behind.
- `feedback.md` — chronological execution and verification log.

## Selected payload

- active removable-front generator, validator, and handoff update;
- seven previously untracked closure experiments needed by the active lineage;
- pre-existing tracked transitive source closure;
- enclosure recovery and both deferred-design records;
- the immutable `releases/enclosure_v1` payload;
- thirteen hash-bound evidence files promoted into component-local
  `reference_evidence/` directories;
- focused catalog additions for one release and seven experiments.

The canonical working-set links now resolve to the durable evidence copies
rather than ignored `build/` paths.
