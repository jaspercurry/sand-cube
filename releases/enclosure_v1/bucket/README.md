# Bucket V1 — pending

This folder is reserved for the separately reviewed bucket V1 release. The
front baffle is already frozen under `../front_baffle/` and is out of scope for
bucket changes.

When the bucket is approved, mirror the front-baffle layout with `artifacts/`,
`source/`, and `verification/` subdirectories plus a manifest and checksums.
